package com.humam.challengechapter4

//enum class PlayerPick(val value: Int) {
//    BATU(0),
//    KERTAS(1),
//    GUNTING(2);
//    companion object { // companion object adalah object yang berada di dalam class itu sendiri (tidak perlu di definisikan) dan bisa diakses dengan cara PlayerPick.BATU, PlayerPick.KERTAS, dan PlayerPick.GUNTING
//        fun fromInt(value: Int) = values().first(){it.value == value} // first() untuk mengambil nilai dari enum yang pertama saja (batu) dari array of enum PlayerPick (batu, kertas, gunting)
//    }
//} //berisi list dari batu, kertas, gunting yang bisa dipilih oleh player dan computer.